{{- if .Values.ingress.enabled -}}
apiVersion: networking.k8s.io/v1
kind: IngressClass
metadata:
  name: {{ include "guestbook.fullname" . }}-ingress
spec:
  controller: "ingress.k8s.aws/alb"
{{- end }}